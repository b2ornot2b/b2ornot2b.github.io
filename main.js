// Generated by CoffeeScript 1.9.2
window.offerClicked = function(event) {
  var offerText, rtc;
  console.log('offerClicked', event);
  offerText = document.querySelector('#offer').value;
  console.log('offerText', offerText);
  rtc = RTC({
    room: 'b2ornot2b',
    capture: false
  });
  return rtc.on('ready', function(session) {
    session.createDataChannel('chat', {
      ordered: true,
      maxRetransmits: 12
    });
    return session.on('channel:opened:chat', function(id, channel, attributes, connection) {
      console.log('channel:opened:chat', id, channel, attributes, connection);
      return channel.onmessage = function(event) {
        return console.log('msg: ', event.data);
      };
    });
  });
};

//# sourceMappingURL=main.js.map
